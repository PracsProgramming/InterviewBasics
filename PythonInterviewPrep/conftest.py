#conftest
# all files have access to conftest
# fixtures are defined in conf tests
#centralized file where common methods are written


#how to execute only failed tests
#pytest --last-failed
#pytest # all tests
#pytest -n 3 # 3 in parallel


#custome marker
#@pytest.mark.sanity
#pytest -m sanity

